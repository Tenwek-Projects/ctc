; Raise PHP upload limits for admin media (gallery, team photos, etc.).
; Shared hosts that honor .user.ini typically pick this up after a short delay.
upload_max_filesize = 40M
post_max_size = 48M
max_file_uploads = 40
